import GoalsContent from "@/components/onboarding/goals/goals-content";
import CustomContainer from "@/components/reusable/CustomContainer";

const goal = () => {
  return (
    <CustomContainer>
      <GoalsContent />
    </CustomContainer>
  );
};

export default goal;
